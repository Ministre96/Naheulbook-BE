spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/naheulbookDB
    username: postgres
    password: admin
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update